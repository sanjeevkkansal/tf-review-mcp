# mcp-adversarial

Adversarial input harness for MCP servers. Placeholder package; real content lands in `mcp-adversarial-v0.1.0` (see the root [V0_4_PLAN.md] for the roadmap; the public surface stabilizes in PR #1 / PR #1.5).

This directory exists ahead of that release so the workspace layout is in place and tf-review-mcp can begin importing the harness as a dev dependency without further repo restructuring.
