[project]
name = "tf-review-mcp-workspace"
version = "0"
description = "Workspace root for tf-review-mcp and mcp-adversarial."
requires-python = ">=3.11"

[tool.uv]
package = false

[tool.uv.workspace]
members = ["packages/*"]

[tool.ruff]
line-length = 100
target-version = "py311"
